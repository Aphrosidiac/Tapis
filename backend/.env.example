DATABASE_URL=postgresql://user:password@localhost:5432/tapis
PORT=3140
JWT_SECRET=change-me-to-a-long-random-string
NODE_ENV=production
# Optional. Can also be entered on the Settings screen (stored encrypted).
ANTHROPIC_API_KEY=
# Optional. Encrypts stored secrets; generated to .credential-key if unset.
CREDENTIAL_KEY=
